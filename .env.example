# Device: "cpu" or "cuda" (auto-detected by run.sh)
# VOICEBOX_DEVICE=cpu

# Exposed port (default: 17493)
# VOICEBOX_PORT=17493

# Model to auto-download on first run (set empty to skip)
# VOICEBOX_MODEL=chatterbox-tts
