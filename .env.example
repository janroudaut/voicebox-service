# Device: "cpu" or "cuda" (auto-detected by run.sh)
# VOICEBOX_DEVICE=cpu

# Exposed port (default: 17493)
# VOICEBOX_PORT=17493

# Data directory: local path or Docker volume name (default: Docker named volume)
# VOICEBOX_DATA=./data
